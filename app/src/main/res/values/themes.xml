<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CST2335FinalProject" parent="Theme.Material3.DayNight.NoActionBar"> // no action bar on the top

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <item name="colorPrimary">@color/flight_primary</item>
        <item name="colorSecondary">@color/flight_secondary</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">@color/flight_statusbar</item>
        <item name="android:textColor">@color/flight_text</item>

    </style>

    <style name="Theme.CST2335FinalProject" parent="Base.Theme.CST2335FinalProject" />
</resources>